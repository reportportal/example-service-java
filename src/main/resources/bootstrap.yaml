info:
  build:
    name: My Service
    description: My Service Description
    version: '@version@'
    branch: '@branch@'
    repo: '@repo@'

spring:
  application:
    name: my-service
  profiles:
    active: ${rp.profiles:default}
security:
  sessions: stateless
  basic:
    enabled: false
